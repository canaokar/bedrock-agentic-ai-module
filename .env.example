# Copy this file to .env and paste your token:
#   cp .env.example .env
#
# Then edit .env and replace the value below with your actual token.
# All lab scripts will pick it up automatically — no need to export.

AWS_BEARER_TOKEN_BEDROCK=paste-your-token-here
